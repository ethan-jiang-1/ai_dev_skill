# 审查、发布、运维与状态持久化

## 这份片段在讲什么

这份上下文聚焦 SDLC 后半段和底层支撑能力：

- Review 阶段怎么避免模型自我审查失真
- Ship / Operate 阶段怎么把 QA、发布、监控和文档同步自动化
- 全生命周期怎么通过文件状态持久化避免“空白石板”问题

## 从原始研究提炼出的核心结论

### 1. Review 阶段最致命的问题是“同一个模型审自己”

原始研究把这个问题讲得很透：

- Builder 再去 review 自己生成的代码，会产生回音壁
- LLM 会倾向于认可自己的逻辑
- 结果是安全漏洞和边界问题被系统性漏掉

因此最值得研究的机制不是“多做 review”，  
而是“把 review 从 builder 身上解耦”。

### 2. 对抗性审查是高价值能力单元

原始研究重点讨论了：

- `Adversarial Critic`
- `CONSENSAGENT`
- 盲审 + 魔鬼代言人机制

这些机制的共同点是：

- builder 结束后，critic 用全新上下文接手
- critic 只看需求和 diff，不继承 builder 的思路
- 审查不是泛泛点评，而是绑定打分卡和 blocking 问题

这类能力单元的价值非常高，  
因为它为自动化开发链条引入了必要的制衡机制。

### 3. Ship / Operate 阶段的关键是“从代码文本走向真实运行时”

原始研究在 gstack 的 `/qa`、`/ship`、`/canary`、`/benchmark` 这些单元上给出了很强的例子。

最值得提炼的点是：

- 用真实 Playwright 无头浏览器做交互验证
- 用视觉差异、控制台报错、性能指标做运行时 QA
- 发布后继续做 canary 和 benchmark 监控

这说明一个成熟能力单元不该只停留在 repo 内部，  
而要穿透到真实运行环境。

### 4. 文档同步本身也是重要能力单元

原始研究提到 `document-release` 这一类对象时，给了一个很实用的提醒：

- 代码变了，文档不更新，是很典型的漂移问题

因此，文档同步不只是收尾动作，  
而是发布闭环的一部分。

### 5. 文件型持久化记忆是目前最可靠的项目记忆机制

原始研究对 `PROJECT.md`、`STATE.md`、`PLAN.md`、`DECISIONS.md` 这一套模式评价非常高。

核心观点是：

- 不要指望 LLM 聊天记忆来保存项目状态
- 结构化 Markdown 文件才是高保真的状态机载体

这套机制的价值在于：

- 新智能体启动时可以快速重建上下文
- 项目决策能够被持续沉淀
- 支持回滚、溯源和阶段切换

这不是附属优化，  
而是全生命周期稳定性的底层支撑。

## 这一片段里最值得继续研究的对象

- Adversarial Critic / CONSENSAGENT
- Headless Browser QA
- `/ship`、`/canary`、`/benchmark`
- `document-release`
- `PROJECT.md` / `STATE.md` / `PLAN.md` / `DECISIONS.md`

## 适合继续 Deep Research 的问题

- 对抗性代码审查有哪些成熟实现模式
- Review 阶段如何设计真正有效的 blocking 机制
- 发布和运维能力单元怎么从“能跑”升级到“可观测、可回归、可防漂移”
- 基于文件的状态持久化，为什么比向量记忆或聊天历史更可靠

## 这一片段的用途

如果下一轮 Deep Research 想回答下面这些问题，这份片段最适合直接当上下文：

- “AI Agent 在 Review / Ship / Operate 阶段最值得借鉴的能力单元是什么？”
- “怎么用对抗性审查打破模型的自我确认偏差？”
- “为什么文件型状态持久化是 agent 项目记忆的基础设施？”
