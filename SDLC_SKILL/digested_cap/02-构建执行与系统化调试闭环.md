# 构建执行与系统化调试闭环

## 这份片段在讲什么

这份上下文聚焦 SDLC 中间最容易失控的部分：

- Build 阶段怎么防止上下文腐化
- Test / Debug 阶段怎么避免模型进入盲猜和无限重试

## 从原始研究提炼出的核心结论

### 1. Build 阶段的核心问题不是“能不能写代码”，而是“能不能在长周期里持续写对”

原始研究指出，编码阶段真正的灾难来源是：

- 上下文腐化
- 变量和约束遗忘
- 长会话导致的智力基线下降

为了解这个问题，GSD 采用了一个非常强的结构：

- `Orchestrator`
- `Fresh Subagents`
- `Wave-Based Execution`

核心思想是：

- 编排器只负责调度，不亲自写代码
- 写代码的 worker 总是新的、纯净的上下文
- 任务按依赖拆成 wave 并行执行

这不是小优化，而是对进程模型的重构。

### 2. 编排与执行分离，是对抗 context rot 的最强抽象之一

原始研究把 GSD 的这套机制评价得很高，原因在于它直接解决了一个物理限制：

- 同一个超长上下文既决策又执行，质量一定会下滑

而 “编排器 + 新鲜子智能体” 的模式带来的好处是：

- Task 1 和 Task 50 的执行质量更接近
- 长会话积累的“思维垃圾”不会污染后续任务
- 可以自然支持并发波次执行

这类机制的迁移价值很高，  
因为它不依赖具体语言或技术栈。

### 3. 强制 TDD 的真正价值在于“改变模型习惯”

原始研究里的另一个关键对象是 `superpowers` 的 `RED-GREEN-REFACTOR` 强制闭环。

它最值得研究的不是“TDD 很重要”这种常识，  
而是它如何通过流程和回滚机制强迫模型遵守：

- 先写失败测试
- 再写最小实现
- 最后再重构

如果模型试图跳过测试、先写业务逻辑，  
系统会直接删除或回滚这部分代码。

这说明能力单元真正强的时候，  
不是“提醒你应该 TDD”，而是“不允许你不 TDD”。

### 4. 调试阶段最重要的是剥夺模型“马上改代码”的权限

原始研究对 `superpowers` 的 `4-Phase Systematic Debugging` 评价很高。

它的关键不是多了四步，而是执行权限被重排：

- 先收集信息
- 再隔离问题
- 再提出根因假设
- 最后只允许通过小范围验证来证伪或证实

在确认根因之前，模型不能碰生产代码。

这个抽象非常重要，  
因为它直接克制了 LLM 的急躁和瞎猜天性。

### 5. RARV Cycle 代表了“反思 + 验证 + 熔断”的另一条路线

原始研究还提到 GABBE / Loki Mode 一类的 `RARV` 循环：

- Reason
- Act
- Reflect
- Verify

这套机制值得继续研究的重点有两个：

- `Verify` 交给确定性脚本，不交给 LLM 自己判断
- 重试次数有上限，超限就熔断并要求人工介入

这说明高级能力单元不仅会验证，  
还会在自动化失败时优雅退出，而不是无限消耗资源。

## 这一片段里最值得继续研究的对象

- GSD 的 `Orchestrator + Fresh Subagents`
- Wave-based execution
- `superpowers` 的 `RED-GREEN-REFACTOR`
- `4-Phase Systematic Debugging`
- `RARV Cycle`

## 适合继续 Deep Research 的问题

- 长会话 AI 编程为什么一定会发生 context rot，如何从架构上缓解
- 编排器与执行器分离模式有哪些变体
- 强制 TDD 和系统化调试机制里，哪些部分最容易迁移
- 什么时候该让系统自动重试，什么时候必须熔断交给人

## 这一片段的用途

如果下一轮 Deep Research 想回答下面这些问题，这份片段最适合直接当上下文：

- “AI Agent 在 Build / Test / Debug 阶段最值得借鉴的能力单元是什么？”
- “如何设计一个不会越修越坏的 AI 调试系统？”
- “为什么上下文净化和强制 TDD 是 AI 编程质量的关键护栏？”
